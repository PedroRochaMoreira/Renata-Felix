import{NextResponse}from'next/server';import{requireAdmin}from'../../../../lib/auth';import{deleteCatalogProduct,setStock}from'../../../../lib/store';
export async function PATCH(req:Request){try{await requireAdmin();const{id,stock}=await req.json();if(!id||!Number.isFinite(Number(stock)))return NextResponse.json({error:'Dados de estoque inválidos.'},{status:400});return NextResponse.json({inventory:setStock(id,Number(stock))})}catch{return NextResponse.json({error:'Acesso administrativo necessário.'},{status:403})}}
export async function DELETE(req:Request){try{await requireAdmin();const{id}=await req.json();if(!id)return NextResponse.json({error:'Produto não informado.'},{status:400});deleteCatalogProduct(id);return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Acesso administrativo necessário.'},{status:403})}}
